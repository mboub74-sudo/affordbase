import Link from 'next/link';import {notFound} from 'next/navigation';import Calculator from '../components/Calculator';import TaxCalculator from '../components/TaxCalculator';import {pages,getPage} from '../data';
export function generateStaticParams(){return pages.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}){const {slug}=await params,p=getPage(slug);if(!p)return {};return {title:`${p.title} | AffordBase`,description:p.description,alternates:{canonical:`/${p.slug}/`}}}
export default async function Page({params}){const {slug}=await params,p=getPage(slug);if(!p)notFound();const type=p.type==='city'?'rent':p.type;const region=slug.includes('ontario')?'ON':slug.includes('quebec')?'QC':slug.includes('california')?'CA':slug.includes('texas')?'TX':undefined;const country=slug.includes('california')||slug.includes('texas')?'US':'CA';return <><header><Link className="brand" href="/">AffordBase<span>.</span></Link><nav><Link href="/rent-affordability-calculator/">Rent</Link><Link href="/hourly-to-salary-calculator/">Salary</Link><Link href="/car-affordability-calculator/">Car</Link></nav></header><main className="landing"><div className="crumb"><Link href="/">Home</Link> / {p.title}</div><section className="pagehero"><div className="eyebrow">FREE AFFORDABILITY TOOL</div><h1>{p.h1}</h1><p>{p.description} Change the numbers below to see the estimate instantly.</p></section>{p.type==='tax'?<TaxCalculator initialCountry={country} initialRegion={region}/>:<Calculator type={type}/>}<section className="content"><h2>How this calculator works</h2><p>AffordBase turns your inputs into a planning estimate. It is designed to help you compare scenarios quickly, not to replace an official tax return, lender decision, lease qualification, or professional financial advice.</p><h2>Explore related calculators</h2><div className="linkgrid">{pages.filter(x=>x.type===p.type&&x.slug!==p.slug).slice(0,6).map(x=><Link key={x.slug} href={`/${x.slug}/`}>{x.title} →</Link>)}</div></section></main><footer><b>AffordBase.</b><span>Practical affordability estimates for everyday decisions.</span></footer></>}
