import Link from 'next/link';import {notFound} from 'next/navigation';import {CITY_DATA,cityMonthlyCost} from '../../cityData';import {CITY_SLUGS,citySlug} from '../../citySlugs';import {canadaTax,usaTax} from '../../tax';import {SITE_URL} from '../../site';
const pairs=[['Toronto','Calgary'],['Toronto','Vancouver'],['Toronto','Montreal'],['New York City','Miami'],['New York City','Chicago'],['Los Angeles','Miami']];
export function generateStaticParams(){return pairs.map(([a,b])=>({pair:`${citySlug(a)}-vs-${citySlug(b)}`}))}
function resolve(pair){return pairs.find(([a,b])=>`${citySlug(a)}-vs-${citySlug(b)}`===pair)}
export async function generateMetadata({params}){const {pair}=await params,p=resolve(pair);if(!p)return{};return{title:`${p[0]} vs ${p[1]} Cost of Living (2026) | AffordBase`,description:`Compare ${p[0]} and ${p[1]} rent, modeled living costs and salary affordability in 2026.`,alternates:{canonical:`/compare/${pair}/`}}}
export default async function Page({params}){const {pair}=await params,p=resolve(pair);if(!p)notFound();const [a,b]=p;const render=n=>{const c=CITY_DATA[n],salary=90000,t=c.country==='CA'?canadaTax(salary,c.region):usaTax(salary,c.region),cost=cityMonthlyCost(c),left=t.net/12-cost,fmt=x=>new Intl.NumberFormat('en-US',{style:'currency',currency:c.currency,maximumFractionDigits:0}).format(x);return <article className="citycard"><div className="cityhead"><div><span>{c.country==='CA'?'CANADA':'USA'}</span><h3>{n}</h3></div></div><div className="cityhero"><span>Money left on {fmt(salary)} salary</span><strong>{fmt(left)}</strong><small>/mo</small></div><div className="costrows"><p><span>Take-home</span><b>{fmt(t.net/12)}</b></p><p><span>Rent</span><b>{fmt(c.rent)}</b></p><p><span>Total modeled costs</span><b>{fmt(cost)}</b></p></div><Link className="citycta" href={`/cities/${citySlug(n)}/`}>Open {n} calculator →</Link></article>};const breadcrumb={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:`${SITE_URL}/`},{"@type":"ListItem",position:2,name:`${a} vs ${b}`}]};return <main className="landing"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(breadcrumb)}}/><div className="crumb"><Link href="/">Home</Link> / Compare / {a} vs {b}</div><section className="pagehero"><span className="pill">CITY COMPARISON</span><h1>{a} vs {b}: Cost of Living</h1><p>A 2026 planning comparison using the same $90,000 local-currency salary scenario in each city.</p></section><div className="comparegrid">{render(a)}{render(b)}</div><section className="content"><h2>How to use this comparison</h2><p>The cards use each city's tax model and cost baseline. Currency is local to each city, so cross-country comparisons are not exchange-rate comparisons. Open either city calculator to model your own rent and savings target.</p></section></main>}
